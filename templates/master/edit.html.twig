<!DOCTYPE html>

<title>Edit Master</title>

{% block body %}
    <h1>Edit Master</h1>

    {{ include('master/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('master_index') }}">back to list</a>

    {{ include('master/_delete_form.html.twig') }}
{% endblock %}
