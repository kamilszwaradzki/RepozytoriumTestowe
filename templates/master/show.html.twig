<!DOCTYPE html>

<title>Master</title>

{% block body %}
    <h1>Master</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ master.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ master.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('master_index') }}">back to list</a>

    <a href="{{ path('master_edit', {'id': master.id}) }}">edit</a>

    {{ include('master/_delete_form.html.twig') }}
{% endblock %}
